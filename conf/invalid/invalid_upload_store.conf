# 'upload_store' points at a directory that does not exist.
server {
    listen 127.0.0.1:8090;
    root   ./www;

    location /upload {
        methods POST;
        upload_store ./no-such-directory;
    }
}
